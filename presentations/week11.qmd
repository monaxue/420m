---
title: "11: Power"
subtitle: "Take two note cards"
---

```{r}

library(tidyverse)
library(ggplot2)

df1 <- tibble(group=c("A","B"), Means=c(1,2), LowerCI = c(0.6,1.6), UpperCI=c(1.4, 2.4))
df2 <- tibble(group=c("A","B"), Means=c(1,2), LowerCI = c(0.6,1.4), UpperCI=c(1.6, 2.4))
plot1 <- ggplot(df1, aes(x=group, y=Means)) + geom_point() + geom_errorbar(aes(ymin=LowerCI, ymax=UpperCI))
plot2 <- ggplot(df2, aes(x=group, y=Means)) + geom_point() + geom_errorbar(aes(ymin=LowerCI, ymax=UpperCI))
```


## Outline
- Case study 2
- Review CIs
- Review p-values
- Introduce power
- Self guided deliverables


## Case study 2
- Overall: Huge improvement from Case Study 1
- People did well on:
    - p-value definition
- Challenges:
    - Not using descriptive statistics to support result
    - CI definition
    - CI interpretations
    - p-value interpretations

## Case Study 2

- I realized that I wasn't clear about my expectations for the interpretations
- I was more flexible on the interpretations for case study 2, I didn't take off points even if you missed important aspects of the interpretations
- So, please read through your feedback carefully, as I will be more strict about interpretations for future case studies.

## Case study 2

- Revisions: Earn up to 14 points this time
    - I will only grade revisions for those who received below 14 points

## Confidence interval definition

Write your name on the note card. Draw the definition of a CI. Pass them back to me.

. . .

![](/assets/ci_drawings_week11.png)

# Confidence Interval Interpretations

## Precise language is important

- Do confidence intervals imply probability?
- The terms you choose to use in your interpretations must demonstrate this understanding
- Which of these terms imply probability?
    - Likely
    - Confident
    - Probable
    - Suggests

## 3 questions when interpreting CIs

1. Do they overlap? And what does that mean?
2. Are your interpretations precise? 
3. What does this say about the true population?

## Practice

::: iframe
https://wall.sli.do/event/ib94xnXY8hNMrXtzmNjiHx
:::

<!--

Write a CI interpretation for the following.

Is there a difference between the two means?

```{r}

plot1

```

- Since the confidence intervals don't overlap, we are confident that the the true means are different.

## Practice

Write a CI interpretation for the following.

Is there a difference between the two means?

```{r}

plot2

```

- Since the confidence intervals do overlap, we are not confident that the the true means are different.


## Practice

Write a CI interpretation for the following.

Is group A different from a threshold value of 0.8?

```{r}

plot1 + geom_hline(yintercept=0.8, color="firebrick", linetype=2)

```

- Since the CI overlaps with/includes the threshold value of 0.5, we cannot be confident that true mean of group A is didfferent from 0.5.

## Practice

Write a CI interpretation for the following.

Is group A different from a threshold value of 1.5?

```{r}

plot1

```

- Since CI does not overlap with/includes the threshold value of 0.5, we can be confident that true mean of group A is didfferent from 1.5.

-->

# p-value review

## Draw the definition of a p-value

Write your name on a note card. Draw the definition of a p-value. Pass them back to me.

. . .

![](/assets/p-value_drawings_week11.png)



## 

1. Assumes null hypothesis is true
2. Create a sampling distribution from the null hypothesis
3. Compare actual observed difference to the null sampling distribution
4. The area under the curve *outside* of the observed differences line is the p-value

# p-value interpretaions

1. What's the probability?
2. Probability of what? 
3. Assuming the null.

# Practice

::: iframe
https://wall.sli.do/event/ib94xnXY8hNMrXtzmNjiHx
:::

# Power

## 

- Power is the opposite of a alpha, the predicted p-value

. . . 

|| p-value | alpha | Power |
|---|---|---|---|
|Sampling distribution|Assumes null hypothesis|Assumes null hypothesis|Assumes alternative hypothesis|
|Cutoff|Observed value (transformed to t-statistic)|Subjectively chosen by us|Determined by alpha|

## {.center}

::: card-list-center
- So how do we determine the cuttoff for alpha and power?
:::

<!--

## Remember circular reasoning?
- Probability of observing a difference if we assume a difference $\leftarrow$ circular (power)
- Probability of observing a difference if we assume the null $\leftarrow$ not circular (p-value)


- This is why we cannot use power as evidence for our hypothesis.

-->

##

##

```{r}

# re-define here because we'll be playing with these
sd_Aus_income <- 20000 # mainly to remind myself
samp_size <- 500
cool_effect <- 5000
se_diff <- sqrt(2)*(sd_Aus_income)/sqrt(samp_size)
criterion <- qnorm(0.95, 0, se_diff) # cool_effect/2


# tibble just for setting up x-axis of plot
x_ax_min <- -3*se_diff
x_ax_max <- cool_effect+3*se_diff
my_XLim = c(x_ax_min, x_ax_max)
xAx <- tibble(x = my_XLim) # x axis limits

### null universe

nullPlot <- ggplot(xAx, aes(x = x)) + 
  stat_function(fun = dnorm, 
                args = list(mean = 0, sd = se_diff), color = "pink") +
  xlab("Income") +
  ylab("# households") +
  ggtitle("Sampling Distribution of the Null")

### alternate universe

altPlot <- ggplot(xAx, aes(x = x)) + 
  stat_function(fun = dnorm, 
                args = list(mean = cool_effect, sd = se_diff), color = "green") +
  xlab("Income") +
  ylab("# households") +
  ggtitle("Sampling Distribution of the Alternative")


show(nullPlot / altPlot)
```

##

```{r}

# fill alpha
x_lim_fill <- c(criterion, x_ax_max)
nullPlot <- nullPlot + 
  geom_vline(xintercept = criterion) +
  stat_function(fun = dnorm,
                args = list(mean = 0, sd = se_diff), 
                xlim = x_lim_fill, 
                geom = "area",
                fill = "pink", alpha = 0.5)

show(nullPlot / altPlot)

```

##

```{r}

# fill false alarm rate
x_lim_fill <- c(criterion, x_ax_max)
altPlot <- altPlot + 
  geom_vline(xintercept = criterion) +
  stat_function(fun = dnorm,
                args = list(mean = cool_effect, sd = se_diff), 
                xlim = x_lim_fill, 
                geom = "area",
                fill = "green", alpha = 0.2)


show(nullPlot / altPlot)
```

<!--

# So what we can use power for?

- If we compare the actual observed difference with the alternative hypothesis, that is circular.
- So 

- Power lets us deterime what sample size to use for our experiment
- If we want to detect a difference of [insert number], what sample size do we need?


# Summary

:::::: columns

::::: column


p-value
- Null hypothesis: The difference is 0 $\leftarrow$ assumed to be true
- Alternative hypothesis: There is a difference

:::::

::::: column

Power
- Null hypothesis: The difference is 0
- Alternative hypothesis: The difference is [pre-defined number of meaningful size] $\leftarrow$ assumed to be true

:::::

::::::

-->

## Deliverables

[Week11Power.Rmd](/_scripts/Week11Power.Rmd){.filelink}